javascript: (function () { let name = prompt("Please provide a friendly name for this area:"); if (!name) return; let latLonDiv = document.querySelector('.map-info > div:nth-child(1) > div:nth-child(1)'), latLonText = latLonDiv.textContent, [lat, lon] = latLonText.split(', ').map(parseFloat); if (!isNaN(lat) && !isNaN(lon)) { let newUrl = `https://watershed.attack-kitten.com/?name=${encodeURIComponent(name)}&lon=${lon}&lat=${lat}`; window.open(newUrl, '_blank'); } else { console.error('Invalid latitude and longitude values'); } })();